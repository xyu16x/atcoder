## 各桁の判定
n>0である限り、10で割った余りを記録する
e.g. n=380のとき、0あまり38, 3あまり8, 8あまり0

## とある数値に対して、1が1個, 2が2個, 3が3個になっているか判定する
文字列としてソートして、122333と同値になるかチェックする

## ビットシフト演算
2進数の計算に用いると高速
ans = 0
for i in range(64):
    ans += a[i] << i
print(ans)